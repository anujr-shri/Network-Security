[project]
name = "network-security"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anujr_shri", email = "shrivastavaanuj059@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
network-security = "network_security:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
